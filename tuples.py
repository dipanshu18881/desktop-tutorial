#TUPLES - cannot be modified once assigned.
#numbers = (1, 2, 3)
#print(numbers[0])

coordinates = (1, 2, 3)
#x = coordinates[0]
#y = coordinates[1]
#z = coordinates[2]
#same as above
#unpacking the tuple to variables, can be used with lists
x, y, z = coordinates
print(z)